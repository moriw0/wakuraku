<% if @user.kept? %> 
  <div class="user-section">
    <div class="wrapper">
      <div class="user-detail">
        <%= image_tag avatar(@user), class: 'user-image' %>
        <p class="user-name"><%= @user.nickname %></p>
        <p class="user-profile"><%= @user.profile %> </p>
        <%= link_to 'プロフィールを編集する', edit_user_registration_path, class: 'user-edit-link' if current_user?(@user) %>
      </div>
    </div>
  </div>
  <div class="events-section">
    <div class="wrapper">
      <% if @events.any? %>
        <h1><%= @user.nickname %>のココロミ</h1>
        <%= render partial: 'events/event_item', collection: @events, as: 'event' %>
      <% end %>
      <%= paginate @events %>
    </div>
  </div>
<% else %> 
  <p>退会されたユーザーです。</p>
<% end %> 
